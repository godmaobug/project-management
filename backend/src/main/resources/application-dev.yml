server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/projmaster_dev?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: root
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
  expiration: 86400000

logging:
  level:
    com.projmaster: DEBUG
    org.springframework.security: DEBUG
